[project]
name = "vkernelrs"
dependencies = ["cffi"]

[build-system]
requires=["maturin>=1.0,<2.0"]
build-backend = "maturin"

[package.metadata.maturin]
name = "vkernelrs"

[tool.maturin]
compatibility = "linux"
