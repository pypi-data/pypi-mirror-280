[build-system]
requires = ["maturin>=0.14,<=0.16"]
build-backend = "maturin"

[project]
name = "pywavers"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "numpy>=1.21",
]
readme = "README.md"

[project.optional-dependencies]
test = [
    "soundfile>=0.10",
]

[tool.maturin]