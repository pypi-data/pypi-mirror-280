[project]
name = "globy_core"
dynamic = ["version"]
authors = [
  { name="Björn L", email="bjorn@globy.ai" },
]
description = "Globy Core"
readme = "README.md"
requires-python = ">=3.9"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]

dependencies = [
  "colorlog",
]

[build-system]
requires = ["setuptools>=45", "setuptools_scm[toml]>=6.2"]

[tool.setuptools_scm]
# none

[project.urls]
Homepage = "https://github.com/globyai"

[project.scripts]
# globy-test1 = "globy_core.main:main"
