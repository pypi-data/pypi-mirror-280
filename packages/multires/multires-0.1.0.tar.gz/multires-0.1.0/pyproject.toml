[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "multires"
description = "Implementation of the standard and hybrid multiresolution algorithm to generate first passage times and trajectories"
version = "0.1.0"
readme = "README.md"
authors = [
    { name = "Juan Magalang", email = "jarmsmagalang@gmail.com" }
]
dependencies = [
    "numpy"
]