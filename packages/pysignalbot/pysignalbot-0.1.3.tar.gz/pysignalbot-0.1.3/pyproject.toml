[build-system]
build-backend = "poetry.core.masonry.api"
requires = ["poetry-core>=1.0.0"]

[tool.poetry]
authors = ["poterba"]
description = "Python client for `signal-cli-rest-api`"
license = "MIT"
maintainers = ["poterba"]
name = "pysignalbot"
readme = "README.md"
repository = "https://github.com/poterba/pysignalbot"
version = "0.1.3"

[tool.poetry.dependencies]
python = "^3.9"
websockets = "*"
asyncio = "*"
requests = "*"
dataclass-wizard = "*"

[tool.poetry.dev-dependencies]
black = "^22.1.0"
