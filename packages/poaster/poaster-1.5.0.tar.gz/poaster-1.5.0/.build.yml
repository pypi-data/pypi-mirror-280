image: alpine/edge
packages:
  - pipx
sources:
  - https://git.sr.ht/~loges/poaster
tasks:
  - lint: |
      cd poaster
      pipx run hatch run check
  - test: |
      cd poaster
      pipx run hatch run cov
