[tool.poetry]
name = "nophp"
version = "0.1.9rc5"
description = "Not Only PHP - A backwards compatible language focused on modern features and flexibility."
authors = ["Artur Z <doslunar8@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
pyyaml = "^6.0.1"
sly = "^0.5"
rich = "^13.7.0"
flask = "^3.0.0"
bcrypt = "^4.1.2"
stripe = "^8.8.0"
resend = "^2.1.0"
qrcode = "^7.4.2"
googlewalletpassgenerator = "^1.1.6"

[tool.poetry.scripts]
nophp = "nophp.spindle:main"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
