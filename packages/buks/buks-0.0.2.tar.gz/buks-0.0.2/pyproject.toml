[tool.poetry]
name = "buks"
version = "0.0.2"
description = "Buks.dev Python Client"
authors = ["Stan Girard <stan@quivr.app>"]
readme = "README.md"
[virtualenvs]
prefer-active-python = true

[tool.poetry.dependencies]
python = "^3.12"
sqlmodel = "^0.0.19"
psycopg2-binary = "^2.9.9"
python-dotenv = "^1.0.1"
stripe = "^9.12.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
