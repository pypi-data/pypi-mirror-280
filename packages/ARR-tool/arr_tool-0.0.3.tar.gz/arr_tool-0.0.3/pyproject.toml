[build-system]
requires=["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ARR_tool"
version = "0.0.3"
authors = [
  { name="DAA" },
]
description = "A tool for making prediction"
readme = "README.md"
requires-python = ">=3.11"
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
]
