#
# Developed by cKnowledge.org and cTuning.org
#

[build-system]
build-backend = "setuptools.build_meta"
requires = ["setuptools>=60", "wheel", "cmind"]

[project]
name = "cm-mlperf"

authors = [
]

description = "CM interface to prepare and run MLPerf inference benchmarks"

requires-python = ">=3.8"

dependencies = [
    "pyyaml",
    "requests",
    "setuptools",
    "cmind"
]

keywords = [
    "cmind",
    "cm_mlperf",
    "cm4mlperf",
    "automation",
    "portability",
    "modularity",
    "cknowledge",
    "ctuning",
    "mlcommons",
    "mlperf"
]

license = {text = "Apache Software License (Apache 2.0)"}

classifiers = [
    "Intended Audience :: Developers",
    "Intended Audience :: End Users/Desktop",
    "Intended Audience :: Science/Research",
    "Intended Audience :: Information Technology",
    "Topic :: Software Development",
    "Topic :: System",
    "Topic :: Scientific/Engineering",
    "License :: OSI Approved :: Apache Software License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3.8",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
]

dynamic = ["readme", "version"]

[project.urls]
Homepage = "https://access.cKnowledge.org"
Source = "https://github.com/mlcommons/cm4mlops"

[tool.setuptools]
zip-safe = false

[tool.setuptools.packages.find]
where = ["."]
include = ["cm_mlperf", "cm_mlperf.*"]
exclude = ["docs", "docs.*", "tests", "tests.*"]

[tool.setuptools.package-data]
cm_mlperf = [
  "internal/**/*"
]

#[tool.setuptools.package-data]
#"cm_mlperf.internal" = ["*.yaml", "*.json", "*.md"]

[tool.setuptools.dynamic]
readme = {file = "README.md", content-type = "text/markdown"}
version = {attr = "cm_mlperf.__version__"}
