# This file should be in the future instead of setup.py
# https://python-poetry.org/docs/pyproject

# It seems we need to copy this file also to serverless-com repo, as required by dialog-workflow-python-package

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.poetry]
name = "group-profile-remote"
version = "0.0.2" # https://pypi.org/project/group-profile-remote i.e. https://pypi.org/project/storage-local/
description = "group-profile-remote Python Package"
readme = "README.md"
authors = [
    "Circlez.ai <info@circlez.ai>",
]
